import { AlphabetsWithoutSpaceDirective } from './alphabets-without-space.directive';

describe('AlphabetsWithoutSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphabetsWithoutSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
